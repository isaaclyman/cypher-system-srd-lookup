import 'package:flutter/material.dart';

class CPageBrowse extends StatelessWidget {
  const CPageBrowse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Browse"),
    );
  }
}
