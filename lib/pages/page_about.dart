import 'package:flutter/material.dart';

class CPageAbout extends StatelessWidget {
  static const name = "About";

  const CPageAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("CYPHER SYSTEM SRD LOOKUP"),
      ],
    );
  }
}
